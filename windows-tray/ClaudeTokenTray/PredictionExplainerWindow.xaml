<Window
    x:Class="ClaudeTokenTray.PredictionExplainerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Projection">

    <Grid>
        <Grid.Resources>
            <SolidColorBrush x:Key="RootBackgroundBrush" Color="#FF18181B" />
            <SolidColorBrush x:Key="TitleBrush" Color="#FFFFFFFF" />
            <SolidColorBrush x:Key="SubtleTextBrush" Color="#FFA1A1AA" />
        </Grid.Resources>

        <Border Background="{StaticResource RootBackgroundBrush}">
            <Grid Padding="16" RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" MinHeight="140" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    x:Name="HeaderText"
                    Grid.Row="0"
                    FontSize="15"
                    FontWeight="SemiBold"
                    TextWrapping="Wrap"
                    Foreground="{StaticResource TitleBrush}" />

                <Canvas
                    x:Name="DetailCanvas"
                    Grid.Row="1"
                    Background="Transparent"
                    SizeChanged="DetailCanvas_SizeChanged" />

                <TextBlock
                    x:Name="SentenceText"
                    Grid.Row="2"
                    FontSize="12"
                    TextWrapping="Wrap"
                    Foreground="{StaticResource SubtleTextBrush}" />
            </Grid>
        </Border>
    </Grid>
</Window>
